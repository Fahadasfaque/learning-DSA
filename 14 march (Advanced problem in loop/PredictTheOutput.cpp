            //PREDICT The OUTPUT


#include <iostream>
using namespace std;

int main() {
    while ('a' < 'b')//97<98
        cout << "malayalam is a palindrome" << endl;
    return 0;
}

        // Output will be infinite because 
        // in while loop a<b means in ascii value 97<98 which is true
        // So, condition is always true that's why "malayalam is a palindrome" will print upto infinite